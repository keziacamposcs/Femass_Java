public class Ex101
{
    public static void main(String[] args) 
    {
        ArrayList<ArrayList<Integer>> matriz = new ArrayList<>();
    //    ArrayList<Aluno> array_aluno = new ArrayList<Aluno>();

        //Matriz
        for(int linha=0; linha<50; linha++)
        {
            for(int coluna=0; coluna<20; coluna++)
            {
                Mtx matriz = new Mtx();

                matriz.add(JOptionPane.showInputDialog("Digite um numero - sem ser negativo - para o tamanho da lista: "));
        }
        //Fim Matriz

        System.out.println(array_aluno);
    }       
}
